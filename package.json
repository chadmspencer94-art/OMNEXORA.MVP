{
  "name": "grok-master-prompt-pack",
  "version": "1.0.0",
  "private": true,
  "description": "Local prompt builder and QA checks for a Grok master prompt workflow.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node scripts/validate-repo.mjs && node --test",
    "smoke": "node --test tests/browser-smoke.test.mjs",
    "debug:prompt": "node scripts/debug-prompt.mjs",
    "serve": "python3 -m http.server 4173"
  }
}
